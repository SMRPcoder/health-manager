first create admin with index file route-"/addAdmin"
and put env variables in .env file
with "/addMedicine" route add the mock medicines to db
with "/addRole" route add "ADMIN","DOCTOR" ,"PATIENT"
with the admin register "Doctor"
Open register for "patients"